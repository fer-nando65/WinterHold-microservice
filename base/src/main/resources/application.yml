spring:
  application:
    name: base

server:
  port: 0

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

gateway:
  url: ${GATEWAY_URL}
